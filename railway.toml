[build]
builder = "nixpacks"

[deploy]
startCommand = "streamlit run app.py --server.port $PORT --server.address 0.0.0.0"
healthcheckPath = "/"
restartPolicyType = "always"

[[mounts]]
source = "client_memories"
destination = "/app/client_memories"
